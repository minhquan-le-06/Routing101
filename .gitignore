.venv/
.idea/
.claude/
__pycache__/
*.pyc

*.log

# FAISS indices, built by the app on each profile's first launch --
# reproducible, and too large to commit.
index/

# Claude-only working notes stay local (ARCHITECTURE.md is the tracked,
# teammate-facing reference).
CLAUDE.md
